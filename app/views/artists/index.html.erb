<h1>All Artists</h1>
<ul>
<% @artists.each do |artist| %>
  <li><%= link_to artist.name, artist %>
    (<%= artist.song_count %> <%= "song".pluralize(artist.song_count) %>)
<% end %></li>
<br />
</ul>



<%= link_to "Add an artist", new_artist_path %><br>
<%= link_to "Songs List", songs_path %>
